/**
 * lens-oracle.ts — Compat re-export
 *
 * A camada de acesso real foi consolidada em `lentes-repository.ts`.
 * Este arquivo existe apenas para preservar imports legados enquanto a
 * migração para `$lib/api/lentes-repository` (ou `$lib/api`) não é concluída.
 */
export * from './lentes-repository';
